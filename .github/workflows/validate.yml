- name: Basic Manifest Validation
        run: |
          # Verifies manifest.json is valid JSON and has required keys
          jq '.' custom_components/hue_smooth_dimming/manifest.json
          grep -q '"domain"' custom_components/hue_smooth_dimming/manifest.json
          grep -q '"version"' custom_components/hue_smooth_dimming/manifest.json
